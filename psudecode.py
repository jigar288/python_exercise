Initialize counter
Def anon_note_checker(message, magazine):
	turn message and magazine into a list 
	do for loop to of message
		inside for loop do if i in magazine then counter +1 
		then do i += 1 to go to the next word
       		inside for loop if i not in magazine then return false 

	call a function here to check if all words in message are matched so do len(message) == 	  
        counter 
	If that statement turns out to be true then return true. 